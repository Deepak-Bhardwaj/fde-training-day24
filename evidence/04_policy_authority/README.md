# Policy and Authority Evidence

These files are deterministic constraints around the AI/agent layer. They are not optional prompt text. The application must enforce them before exposing or committing controlled actions.
